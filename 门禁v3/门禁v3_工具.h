long get_timec();                                      // 时间
int check_string_length(char str[], int min, int max); // 检查字符串长度
int check_string_1(char str[]);                        // 检查字符串由字母或者数字组成
int check_string_2(char str[]);                        // 检查字符串同时包含字母、数字、下划线
int check_phone(char str[]);                           // 检查手机号是否合法